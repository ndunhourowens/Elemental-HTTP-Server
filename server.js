// require/import the HTTP module
var http = require('http');
var PORT = 3000;
var url = require('url');
var qs = require('querystring');
// Create a server
var server = http.createServer(function(request,response){

});

// requestListener is a function that is added to the 'request' event
  // event: 'request'

